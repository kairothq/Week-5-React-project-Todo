// this will have the component which will render all the todos, i.e. putting them on the dom
// we need to export this fn 

export function Todos({todos}){ // this is object destructuring -> i.e. passing all the todos in the form of array as input for rendering 
    return <div>
        todos.map()
    </div>
}

